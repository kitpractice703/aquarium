spring.application.name=Naquarium

# DB 연결 설정 (환경변수가 없으면 기본값 사용)
spring.datasource.url=${DB_URL:jdbc:mysql://aquarium-db.c7mas6skcaol.ap-northeast-2.rds.amazonaws.com:3306/aquarium?serverTimezone=Asia/Seoul&characterEncoding=UTF-8}
spring.datasource.username=${DB_USERNAME:admin}
spring.datasource.password=${DB_PASSWORD:rladlsxo}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA 설정
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# Google OAuth 소셜 로그인 설정
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:927012893267-ips2hk53in133rf6cf0561am951ta7s9.apps.googleusercontent.com}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET: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}
spring.security.oauth2.client.registration.google.scope=email,profile
spring.security.oauth2.client.registration.google.redirect-uri=${GOOGLE_REDIRECT_URI:https://aquarium-livid.vercel.app/login/oauth2/code/google}

# 세션 쿠키 설정
server.servlet.session.cookie.path=/
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false
server.servlet.session.cookie.same-site=lax
server.forward-headers-strategy=framework